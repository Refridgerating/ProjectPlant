[project]
name = "projectplant-care-engine"
version = "0.1.0"
description = "Pure Python care and irrigation logic for ProjectPlant"
requires-python = ">=3.11"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
